import 'package:flutter/material.dart';

class ShapeDecorationDesigner extends StatelessWidget {
  const ShapeDecorationDesigner({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: ListView(),
    );
  }
}
